"""OpenNIW local companion.

The Agent Skill is the brain; this package is the organ it launches: a
localhost browser UI over the case folder, plus headless deterministic
compute (PDF filling, package ZIP, DOCX export, citation harvest).
No accounts, no database, no API keys.
"""

__version__ = "0.3.0"
